// PLACEHOLDER DATA — DELETE THIS FILE WHEN THE ENDPOINTS EXIST.
//
// Nothing is left. Every panel on /metrics now reads a real endpoint, so this
// module exports nothing and the file itself is the last thing to remove,
// along with SampleBadge.vue and the notice on the page.

export {}
