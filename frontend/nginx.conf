# SPA serving for the frontend container (history-mode routes fall back to
# index.html). Production serving is deploy/nginx/whynoipv6.com.conf.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
