# P1 gate — 2026-07-10T10:52:40Z

Full Tranco import + campaign sync on a fresh migrated timescaledb pg18 container:

- v6ctl migrate up: 000001→000003 green; re-run no-op (version 3 clean).
- v6ctl tranco import (live list 46XLX): line_count 1,000,000, imported 999,991, rejected 9,
  duplicates 0, 28.6 s wall. Re-run: no_new_list (idempotent).
- campaign sync (28 real YAMLs, adopt-unknown-uuids, cap 2000 for the 1729-entry Dutch file):
  28 campaigns, 4,502 memberships, 22 junk host entries rejected, 0 rejected files.
  Re-run: 0 created, 0 membership churn (idempotent).

Counts (post-import):
  ranked apexes (rank IS NOT NULL, kind=apex): 999,991  (≈1,000,000)
  domain.tld non-NULL:                        1,003,048 (= every entity)
  total entities:                             1,003,048
  campaign_domain memberships:                4,502 (the spec's ~30k estimate predates repo cleanups)
  subdomains: 622, all with parent_id; created_by: parent_link 115, campaign 2,942

Risk gates: docs/gates/claim-plan-P1.txt (GREEN), docs/gates/resolver-latency-P1.md (GREEN).
make test-integration: green (harness, migrations, claim plan, tranco, campaign, geoip, provider).
